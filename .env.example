# Database Configuration (Optional - uses in-memory storage if not provided)
DATABASE_URL=postgresql://username:password@localhost:5432/cinesync

# Server Configuration
NODE_ENV=production
PORT=5000

# Optional: If deploying to Railway with PostgreSQL addon
# DATABASE_URL will be automatically provided by Railway