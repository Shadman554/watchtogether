# Database Configuration (optional - defaults to in-memory storage)
DATABASE_URL=postgresql://username:password@localhost:5432/cinesync

# Application Environment
NODE_ENV=development

# Server Configuration
PORT=5000
HOST=0.0.0.0

# Optional: External Service API Keys
# Add any external service keys you might need here